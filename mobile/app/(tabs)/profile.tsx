import { View, Text } from "react-native";
import React from "react";
import SafeScreen from "@/components/SafeScreen";

const ProfileScreen = () => {
  return (
    <SafeScreen>
      <Text className="text-white">ProfileScreen n</Text>
    </SafeScreen>
  );
};

export default ProfileScreen;
